server:
  port: 2020

spring:
  application:
    name: xxpay-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
     active: native

security:
  user:
    password: xxpay

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2000/eureka/

logging:
  file: ./log/xxpay-config.log

